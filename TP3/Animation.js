var DEGREE_TO_RAD = Math.PI / 180;

class Animation {
    constructor(scene, animation_id, duration) {
        this.scene = scene;
        this.id = animation_id;
        this.duration = duration;
    }
}